{
  "mcpServers": {
    "dsflow": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/..",
        "--extra",
        "mcp",
        "dsflow",
        "mcp"
      ]
    }
  }
}
